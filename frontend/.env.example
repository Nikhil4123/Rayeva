# ── Backend API ──────────────────────────────────────────
# No trailing slash. For production, use your Render/Railway URL.
VITE_API_URL=http://localhost:3001

# ── Feature flags ────────────────────────────────────────
# Set to "true" to always use mock data (useful for UI-only demos)
VITE_DEMO_MODE=false
