/**
 * This project now uses MongoDB (Mongoose). No SQL migrations needed.
 * Run 'npm run seed' to initialise indexes and demo data.
 */
console.log('This project uses MongoDB. Run "npm run seed" instead.')
process.exit(0)

